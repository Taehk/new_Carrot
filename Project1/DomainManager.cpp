#include "DomainManager.h"

namespace TP
{

	DomainManager::DomainManager() {

	};

	string[10][10] DomainManager::loadListData(string newData, ACTION_KEY actKey) {

	};

	string[10] DomainManager::loadArticleData(string newData, ACTION_KEY actKey) {

	};
}
